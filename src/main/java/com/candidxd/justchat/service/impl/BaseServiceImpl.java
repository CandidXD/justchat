package com.candidxd.justchat.service.impl;import com.candidxd.justchat.dao.BaseDao;import com.candidxd.justchat.service.BaseService;import org.slf4j.Logger;import java.util.List;/** * @author yzk * @Title: BaseServiceImpl * @ProjectName demo * @Description: TODO * @date 2018/8/2111:27 */public abstract class BaseServiceImpl<T> implements BaseService<T> {    /**     * 获取logger     *     * @return org.slf4j.Logger     * @author yaozekai     * @date 2018/8/29 13:25     */    public abstract Logger logger();    /**     * 获得Dao     *     * @return com.demo.dao.BaseDao<T>     * @author yaozekai     * @date 2018/8/23 15:01     */    public abstract BaseDao<T> getMainDao();    @Override    public void add(T t) {        logger().info("===执行ADD方法===");        // logger().info(t.getClass().getName());        // if (jedisUtil.exists(t.getClass().getName())){        //     List<T> list = JSON.parseObject(jedisUtil.get(t.getClass().getName()), new TypeReference<List<T>>(){});        //     list.add(t);        //     jedisUtil.set(t.getClass().getName(), JSON.toJSONString(list));        // }else {        //     List<T> list = new ArrayList<>();        //     list.add(t);        //     jedisUtil.set(t.getClass().getName(), JSON.toJSONString(list));        // }        getMainDao().add(t);    }    @Override    public void update(T t) {        logger().info("===执行UPDATE方法===");        getMainDao().update(t);        // List<T> list = getMainDao().list();        // jedisUtil.set(t.getClass().getName(), JSON.toJSONString(list));    }    @Override    public List<T> list() {        logger().info("===执行LIST方法===");        // Class<T> t = (Class<T>) getClass();        // if (jedisUtil.exists(t.getName())){        //     return JSON.parseObject(jedisUtil.get(t.getName()), new TypeReference<List<T>>(){});        // }        return getMainDao().list();    }    @Override    public void delete(T t) {        logger().info("===执行DELETE方法===");        getMainDao().delete(t);        // List<T> list = getMainDao().list();        // jedisUtil.set(t.getClass().getName(), JSON.toJSONString(list));    }}